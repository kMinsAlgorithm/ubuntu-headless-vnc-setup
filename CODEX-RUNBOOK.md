# Codex 작업 지침: Ubuntu 가상 디스플레이와 VNC

## 1. 대상 PC에서 사용할 시작 프롬프트

아래 폴더 경로를 **압축을 푼 실제 경로**로 바꿔 Codex에게 전달합니다. 이 예시는 설정 작업을 지시합니다. 조사만 원하면 마지막 문장을 “읽기 전용 조사와 적용안 작성까지만 진행해”로 바꿉니다.

```text
이 Ubuntu PC에 모니터 유무에 따른 원격 화면 공유를 설정해줘.

매뉴얼 폴더: /실제/경로/ubuntu-headless-vnc-manual

README.md, MANUAL.md, CODEX-RUNBOOK.md, REFERENCE-STATE.md를 먼저 읽어.
reference/current-pc는 다른 PC의 참고 원본이므로 그대로 설치하지 마.

목표:
- 모니터가 있으면 Ubuntu의 주 모니터 화면을 VNC로 공유.
- 모니터가 없으면 기본 1920×1080 가상 화면으로 같은 사용자 데스크톱 유지.
- 부팅 후 현장 로그인 없이 원격 접속 가능.
- 실행 중 모니터 연결·해제 때 기존 프로그램을 종료하지 않고 화면 전환.
- 외부망에서 접속할 수 있도록 현재 네트워크 경로를 조사하고 구성.
- 사람용 완료 기록과 최종 설정 복사본 작성.

기본은 Ubuntu 22.04이며 24.04에서는 버전별 차이를 확인해.
OS·GPU·실제 출력·사용자·DISPLAY·XAUTHORITY·기존 서비스를 먼저 조사하고,
CHECKLIST.md의 복사본에 대상 PC의 값을 기록해.
이미 필요한 기능이 정상 구성돼 있으면 재설치하지 말고 검증하고 문서화해.

이 요청은 조사, 백업, 대상 PC에 맞는 설정 파일·스크립트 작성과 필요한 설치를 포함해.
사용 중인 작업이 끊길 수 있는 재부팅·로그아웃은 기존 승인과 작업 상태를 확인하고
필요하면 적용 시점만 조율해. 새로운 외부 서비스 계정·인증이 필요하면 그 부분을 알려줘.
기존 원격 복구 경로를 유지하고 비밀번호·개인키·인증 쿠키·토큰을 대화나 문서에 넣지 마.

파일 생성이나 서비스 active만으로 완료라고 하지 말고 실제 통과·실패·미실시를 구분해.
단일 HDMI 참고 구성만 적용한 상태를 다중 모니터 자동 선택 완료라고 보고하지 마.
현재 세션에서 이미 허용한 작업은 반복해서 승인받지 말고 진행해.
목표 달성에 필요한 설정·검증·완료 문서화를 진행해.
```

## 2. 핵심 원칙

1. 사용자의 현재 요청과 기존 승인을 우선한다. 이 문서 자체를 새로운 권한으로 취급하지 않는다.
2. 기준 PC와 대상 PC를 구분한다. `kmg`, UID `1000`, `:0`, `DFP-2`, `HDMI-0`, `PCI:1:0:0`을 상수로 전파하지 않는다.
3. 소프트웨어 버전은 현재 설치 상태와 공식 자료로 확인한다. 정상 드라이버를 이유 없이 교체하지 않는다.
4. VNC로 보여야 하는 것은 실제 로그인한 로컬 사용자 세션이다. 별도 세션을 만들어 목표를 달성한 것으로 보고하지 않는다.
5. 현재 단일 출력 스크립트는 다중 모니터 자동 선택 구현이 아니다. 해당 요구가 있으면 구현하고 검증한다.
6. 디스플레이 공유와 OpenGL/GPU 가속을 별개로 검증한다.
7. 기존 원격 접속을 유지하고, 의도하지 않은 SSH·GPU·로봇 프로세스 중지를 피한다.
8. 비밀번호 입력·외부 서비스 인증은 필요한 사용자 입력으로 처리한다. 비밀값을 임의로 만들거나 보고서에 출력하지 않는다.
9. 모니터 케이블 조작처럼 직접 수행할 수 없는 시험은 사용자 협조를 요청하고 미실시로 남긴다. 성공을 추정하지 않는다.
10. 자동 승인 검토가 거절하면 그 이유를 명시한다. 우회하지 말고 허용되는 작업을 완료하고 남은 제약을 보고한다.

## 3. 실행 단계와 산출물

### 단계 A — 읽기 전용 조사

MANUAL 3장을 수행하고 작업표를 작성한다.

- OS 버전, 데스크톱, 로그인 관리자, Xorg/Wayland.
- GPU·드라이버·출력 담당 GPU·BusID.
- 대상 사용자·UID·홈·DISPLAY·인증 경로.
- 물리 모니터·주 모니터·가상 출력·지원 모드.
- 기존 x11vnc/Xvnc/원격 데스크톱 서비스와 포트.
- 자동 로그인·유휴 잠금·절전·부팅 중 사용자 입력 조건.
- SSH·공유기·VPN·방화벽·IPv6 경로.

비밀 파일 내용은 읽지 않는다. Xorg 로그는 초기 250줄이나 특정 패턴의 제한된 개수만 읽는다. `tailscaled=active`와 VPN 연결 완료를 혼동하지 않는다.

**산출물:** `CHECKLIST.md`의 대상 PC 복사본, 사용할 구성과 남은 불확실성 요약.

### 단계 B — 백업 및 설정 설계

MANUAL 4~10장을 따라 설정 경로를 선택한다.

- 수정할 기존 파일, 신규 파일, 기존 enable/active 상태를 구분해 백업한다.
- 기존 Xorg 설정과 새 파일의 충돌 여부를 확인한다.
- 사용자 자동 로그인, 실제 Xorg 접근 확인, x11vnc 수신 범위를 설계한다.
- 모니터 판별은 DRM·EDID·드라이버 정보를 교차 확인한다.
- 여러 출력이면 primary 선택 및 공유 영역 갱신 규칙을 구현한다.
- 외부 경로는 기존 사용 방식을 먼저 확인한다. 신규 경로 선택이 필요하면 SSH 터널 또는 VPN을 구체적으로 제시하고 필요한 정보만 질문한다.

**산출물:** 대상 PC 값으로 완성한 staging 파일, 변경 이유, 백업·원복 계획.

### 단계 C — 실제 파일 검토

최종 스크립트·서비스·Xorg 파일을 검토한다.

- 문서의 자리표시자와 기준 PC 고정값이 남아 있는가?
- 인증 파일이 단지 존재하는 것이 아니라 선택한 X 서버에 접근되는가?
- 같은 화면에 원격 제어 채널을 공유하는 서버가 중복돼 있는가?
- 물리 화면을 선택할 때 강제 가상 출력이 우선하지 않는가?
- clip 영역이 배치·배율·회전·주 모니터 변경에 따라 갱신되는가?
- 실제 모니터가 없을 때 유효한 가상 출력·모드가 있는가?
- 루프가 상태 변화 없이 계속 모드 설정·서비스 재시작을 반복하지 않는가?
- 실패 시 로그를 남기고 기존 세션·복구 경로를 보존하는가?

가능한 검증: `sh -n` / `bash -n`, 해당 파일의 `systemd-analyze verify`, 지원 옵션·모드 확인. 인증 정보와 display 구성을 다루는 코드를 검증 목적으로 무조건 실행하지 않는다.

### 단계 D — 설치와 적용

완성된 파일을 필요한 경로에 올바른 소유권·권한으로 설치한다. 시스템 서비스와 사용자 서비스를 구분한다.

부팅 때 Xorg가 준비되기 전 서비스가 시작될 수 있으므로 준비 대기·재시도와 실패 로그를 확인한다. 설치·enable만 한 상태와 실제 서비스가 새 파일을 사용 중인 상태를 분리해 기록한다.

이미 승인된 범위에서 적용한다. 사용 중인 데스크톱을 종료해야 하지만 시점이 미정이면 사용자가 검토할 구체적인 변경과 영향을 먼저 준비한 후 적용 시점만 조율한다.

### 단계 E — 실제 시험

MANUAL 13장과 CHECKLIST의 시험표를 사용한다.

- 모니터 없는 부팅, 모니터 있는 부팅.
- 실행 중 연결·해제와 프로그램 유지.
- 주 모니터 변경·다른 출력 포트·화면 좌표.
- 외부망 접속·입력·재접속.
- 잠금·절전·네트워크 복귀.
- 필요한 그래픽 앱·GPU 가속.

현장에서 케이블을 조작하거나 다른 PC에서 접속해야 하는 시험은 실제 협조를 받아 수행한다. 로컬 포트가 열렸다는 사실을 외부 접속 성공으로 대체하지 않는다.

### 단계 F — 인계

**최종 산출물:**

- 대상 PC 값과 실제 시험 결과가 채워진 작업표.
- 적용된 설정·스크립트 복사본. 암호·키 제외.
- 파일 변경 목록과 원복 명령·백업 경로.
- 실제 접속할 주소·포트·뷰어 설정. 비밀번호 제외.
- 실패·미실시 항목과 현재 사용 가능한 기능.

전체 목표를 시험하지 못했다면 “설정 적용 완료, 다음 항목 미검증”처럼 상태를 정확히 보고한다. 실제 하드웨어에서 확인하지 않은 Ubuntu 버전이나 GPU를 지원 완료 목록에 넣지 않는다.
