# 2026-09-18 iPad 화면 키보드 자모 코드 보정

- 사용자가 화면 키보드에서 특수기호가 나온다고 보고했습니다. 당시 복사 보호는 꺼짐(`setclipboard/setprimary=1/1`), VNC 키 입력 허용, Ctrl/Shift 잠금 없음, IBus hangul 상태였습니다.
- 지정한 소량의 키 진단에서 raw keysym `0x3141`, `0x3160`, `0x314a`가 실제 도착했습니다. 기존 보정은 Unicode keysym과 legacy Hangul만 처리하여 이 형식을 놓쳤습니다. 입력 이벤트 개수만으로 정상 문자 입력이라고 판단하지 않습니다.
- 같은 단일 두벌식 자모 33개에 bare 코드 별칭을 추가했습니다. 복합 자모·완성된 음절·일반 Unicode 전체를 재해석하지 않습니다. `직접 사용`의 원래 매핑 복구도 유지했습니다.
- 회귀 검사 **42개 통과**. 별도 VNC/IBus에서 bare 자모→`가나다 빤`과 기존 Mac/물리 iPad Caps·Shift·Cmd 복사·원복이 함께 통과했습니다.
- 설치 후 실제 추가 매핑과 소스 일치를 확인했습니다. 원래 키 설정 백업, Caps 보정 세대, 당시 복사 보호 선택(꺼짐)을 유지했습니다. 실제 VNC/GDM/IBus 재시작이나 Caps 모듈 교체는 하지 않았습니다.
- 수정 후 사용자가 실제 iPad 화면 키보드의 입력이 정상이라고 확인했습니다. 이어 꺼져 있던 복사 보호를 다시 켰으며 키 매핑·Caps 보정 세대가 유지됨을 확인했습니다. 사용자는 이후 정상으로 보인다고 응답했습니다. 최종 상태는 화면 키보드 보정과 복사 보호가 함께 켜진 VNC 모드입니다.

---

# 2026-09-18 iPad RVNC 클립보드 보호

- 실제 iPad RVNC 진단: Cmd+C/V가 Control+c/v로 전달되고 GTK 복사·붙여넣기 동작이 실행됨을 확인했습니다. 고정 문장 `가나다 abc123`이 붙여넣기 후 달라지는 문제를 재현했습니다.
- 같은 기기에서 `setclipboard=0`, `setprimary=0`으로 시험하자 사용자가 **원문 그대로 붙여넣어짐**을 확인했습니다. 5분 시험 종료 후 원래 수신 설정 `1/1`로 복구됨을 확인했습니다.
- 사용자는 **Ubuntu 안에서 정상 복사되면 충분함**을 선택했습니다. 선택형 ‘iPad 복사 보호’를 기존 바탕화면 프로그램에 추가하고 켠 상태로 설치했습니다.
- 오프라인 회귀 검사 **41개 통과**: 새 설정 캡처와 기존 키 백업 보존, 직접 사용 원복, 보호 선택 기억, 적용 실패 복구, 새 VNC 프로세스의 별도 원본 설정, 보호만 바꿀 때 Caps 상태 보존을 포함합니다.
- 별도 Xvfb/loopback VNC 검사: 고정 문자열 덮어쓰기 재현, CLIPBOARD/PRIMARY 보호, 보호 해제 후 수신 재개, 원래 서버 설정 복구 통과. 실제 RVNC의 내부 인코딩 알고리즘을 재현했다고 주장하지 않습니다.
- 기존 모듈을 사용한 격리 VNC/IBus 검사: Unicode/legacy 자모, Mac/iPad Caps 전환, Shift 된소리·영문, 두 번째 Mac 클라이언트, Cmd 복사·붙여넣기, 원래 Caps 복구 통과.
- 설치본 확인: `mode=vnc`, `clipboard_protection=true`, 수신 `0/0`, Caps 잠금 꺼짐, Caps 모듈 켜짐. 기존 키 백업과 실행 중인 Caps 보정 세대가 보존됐고 소스와 설치본이 일치했습니다.
- 기존 VNC/GDM/Xorg/IBus를 재시작하지 않았습니다. 새 코드로 사용자 키보드 watcher와 해당 설정 창만 다시 열었습니다. 원래 파일은 사용자 상태 디렉터리의 설치 백업에 보존했습니다.

보호 중에는 모든 VNC 클라이언트의 기기 → Ubuntu 클립보드 수신이 제한됩니다. 기기 간 한글 복사 전체 호환성을 해결한 기능은 아닙니다. 직접 물리 키보드·새 재부팅·다른 클라이언트 조합의 추가 실기 시험은 하지 않았습니다. 이번 변경 후 실제 Mac에서의 재시험은 별도로 수행하지 않았으며 기존 키 매핑 보존과 격리 시험으로 확인했습니다.

---

# 검증 결과

## 2026-09-18 Mac Cmd 단축키 추가 — 사용자 정상 확인

- Mac 기본 화면 공유에서 전용 진단 창으로 양쪽 Cmd가 모두 Alt_L, Control이 Control_L로 전달됨을 확인. Option은 다른 키로 관측되어 변경 대상에서 제외.
- VNC 모드에 Alt_L→Control_L 추가. 기존 Ctrl·Caps Lock·자모 보정 및 iPad Caps 모듈은 유지. 직접 사용 시 최초 VNC 매핑까지 복구.
- 34개 회귀 검사 통과. 기존 Cmd 매핑과 숫자 별칭 복구, 다른 매핑 보존 확인.
- 격리된 실제 RFB/GTK 시험에서 Cmd+A/C/V로 한글·영문 혼합 문장의 전체 선택·복사·붙여넣기 확인. 기존 iPad Caps 전달, 두 번째 Mac 접속, Shift·한글 조합도 함께 통과.
- 격리된 X11 이벤트에서 Cmd→Ctrl, off 후 원래 Alt 전달 및 Caps 복구 확인.
- 실제 PC에 설치·적용 완료. `mode=vnc`, `caps_bridge=1`, Caps 잠금 꺼짐 유지. VNC·IBus 재시작 없음. 실제 Mac 화면 공유에서 Cmd+A → Cmd+C → 오른쪽 방향키 → Cmd+V 시험 후 사용자가 **“정상적으로 복사·붙여넣기 됨”**으로 확인.

## 2026-09-18 iPad RVNC Caps 상태 보정 — 사용자 정상 확인

- 실제 iPad 이벤트에서 잠금 상태형 Caps 전달과 명시적 Shift를 구분. 이를 그대로 재현한 상태 보정 모듈 구현.
- C 상태 머신을 AddressSanitizer/UndefinedBehaviorSanitizer로 실행: 일반 Mac 한 쌍, iPad 상태 변화, 실제 좌/우 Shift, 키 떼기 대응, 접속자 상태 분리, 끄기·다시 켜기 통과.
- 배포 모듈과 실제 바탕화면 CLI의 on/off를 임시 Xvfb/DBus/IBus/VNC에서 통합 검증. iPad 입력→‘ㅊ c 반 c 빤’, 두 번째 Mac→‘가나다 abc’, 기존 자모·Shift·기호 검사 및 원래 Caps 잠금 복구 통과.
- x11vnc 실행 파일이나 시스템 패키지는 교체하지 않음. 해당 VNC 프로세스에만 선택형 모듈 로드. 원래 실행기 백업·원복과 버전/지문 검사 포함.
- 사용자가 Ubuntu 터미널에서 관리자 인증 후 설치 완료. 실제 새 VNC PID와 준비 신호 일치, 사용자 CLI `mode=vnc`, `caps_bridge=1`, 서버 Caps 잠금 꺼짐 확인. 사용자 키보드 서비스 active.
- VNC만 한 번 재시작. 기존 IBus daemon/engine 프로세스 유지. 화면1600×1050·scale=1, 자동 해상도 watcher disabled/inactive 유지.
- 실제 iPad RVNC 재접속 후 사용자가 **“전환·조합·Shift 모두 정상”**으로 확인. Mac은 앞선 자모 보정 적용 후 사용자 정상 확인을 받았고, 새 모듈에서는 별도 두 번째 Mac 방식 접속을 격리 시험으로 확인.
- 새 재부팅·직접 연결 키보드·다른 Ubuntu/라이브러리 ABI는 미실시. 최초 접속에서 글자 없이 Caps만 여러 번 누르는 경우의 구분 한계는 KEYBOARD-MODE.md에 기록.

## 2026-09-18 Mac 화면 공유 자모 보정 — 사용자 정상 확인

- 실제 Mac 기본 ‘화면 공유’에서 전용 진단 창으로 Caps Lock 네 번을 확인. 모두 Hangul 눌림·떼기가 전달됐고, 후속 입력은 영문/한글 낱자/영문/한글 낱자로 번갈아 도착. 서버 Lock은 모두 꺼짐. 개인 입력 문자와 접속 정보는 레포에 저장하지 않음.
- VNC 모드에 단일 두벌식 자모 33개의 Unicode 및 legacy X11 keysym 보정 추가. 기존 Caps 매핑·외부 매핑·최초 원본 설정을 복구할 수 있음.
- 회귀 검사 33개 통과. 이전 활성 버전에서 갱신·갱신 실패 복구·별칭으로 지정된 기존 자모 매핑 복구를 추가 검사.
- 격리된 실제 VNC/IBus 통합 시험: Unicode 자모→가나다, Shift 자모→빤, legacy 자모→반, 영문 복귀 및 Shift 대문자·기호 통과. Caps 잠금 꺼짐 확인.
- 격리된 Caps Lock 5회·원래 Caps 기능 복구 시험 재통과.
- 현재 PC 설치본에 보정 적용. 기존 VNC/IBus/GDM 재시작 없이 키보드 감시기만 새 코드로 재시작. 최초 원본 백업 유지.
- 수정 적용 후 사용자가 “mac은 정상이지만, ipad는 아직 이슈가 남아있음”이라고 확인. **Mac 조합·전환 정상 확인**, iPad는 별도 미해결. 입력 소스를 자동 동기화하지 않으며 어긋나면 Shift+Space 한 번으로 맞춤.
- 이후 사용자가 iPad 시험 가능하다고 알려 실제 RVNC 전용 창 진단 수행. Caps 이벤트는 down→up→down→up→down으로 교대로 도착하며 사이에 문자 입력이 있음. 서버 Lock은 모두 꺼짐. Caps down 이후 문자에는 인위적인 Shift/대문자가 동반되고, 다음 up 이후에는 소문자가 도착. 실제 Shift+c는 별도 Shift 눌림·떼기로 관측. 네 단계 순환의 전달 원인 확인, 해결은 진행 중.

## 2026-09-18 VNC 키보드 모드 — 클라이언트 확인 진행 중

- 기존 18개 + 키보드 설정/복구 10개 = 28개 오프라인 검사 통과.
- 별도 Xvfb/127.0.0.1 전용 x11vnc에 실제 RFB 키를 보내 Caps Lock→Hangul 5회, Caps 잠금 방지, 소문자·Shift 대문자·Super 보존, 원래 Caps Lock 기능 복구 확인.
- 별도 Xvfb/DBus/IBus/GTK 세션에서 영문 키→‘가나다’ 조합 확인. 실제 사용자 세션의 입력기 재시작 없음.
- 기준 PC에서 바탕화면 아이콘·CLI 설치, Caps 잠금 해제, `Caps_Lock-Hangul` 매핑 적용, `Hangul,Shift+space,Control+space` 전환키 확인.
- 기존 `Control+space,Hangul,Super_R,Alt_L` 값과 VNC 매핑은 로컬 개인 백업에 보존. 실제 직접 연결 키보드 시험은 미실시.
- 키보드 감시기와 해상도 도구의 x11vnc 제어 요청 충돌 발견·수정. 두 도구의 동시 상태 조회 8회 통과. 감시기는 동일 VNC 프로세스에서 주기적으로 제어 속성을 조회하지 않도록 변경.
- 해상도1600×1050·scale=1, 기존 VNC 프로세스·직접 접속 유지. 해상도 선택기의 변경은 제어 통신 잠금 추가이며 프로필 값은 유지.
- 실제 Mac의 입력 언어가 한글인 상태에서 자음·모음 분리가 보고됨. Mac ABC + Shift+Space 결과, VNC 앱별 Caps 전달, iPad, 새 재부팅은 확인 대기/미실시. 서버 시험을 사용자 최종 수용으로 보고하지 않음.

[키보드 사용·복구 절차](KEYBOARD-MODE.md)를 따른다. 아래 기록은 먼저 완료된 해상도 작업 당시의 검사 결과다.

## 2026-09-17 해상도 선택기와 Codex 도구

기준 PC의 실제 iPad 설정을 레포에 추가하고 설치기를 통해 현재 사용자에게 배포했습니다. 설정값·서비스 상태·배포 파일 지문은 [display-profile-state.json](reference/display-profile-state.json)에 기록했습니다.

| 검사 | 결과와 범위 |
|---|---|
| 오프라인 회귀 검사 | 18개 통과. 임시 파일과 모의 화면 사용, 실제 디스플레이 변경 없음 |
| 독립 복구 프로세스 | 실제 별도 프로세스로 keep·revert·EOF·시간 초과 분기 확인. 하드웨어 복구 호출만 대체 |
| CLI 적용 절차 | 모의 화면에서 원본 배율 적용·미리보기 복구·부분 실패 복구·시간 초과·서비스 충돌·동일 설정 무변경 확인 |
| 설치 | 임시 사용자 경로에서 원본 복사·재설치 백업·영수증·실행 권한·심볼릭 링크 거부 확인 |
| 현재 PC 설치 | 파일 백업 후 GUI·아이콘·CLI 설치, 바탕화면 신뢰 표시 성공 |
| 현재 PC CLI | `status` 정상, `apply ipad --keep` → `unchanged` |
| 실제 상태 유지 | 설치 후 35초 이상 경과해도 1600×1050, scale=1, watcher disabled/inactive |
| GUI 원본 | 레포와 기존에 사용자 확인을 받은 설치본의 SHA-256 일치. 이번 패키징에서 GUI 동작 변경 없음 |
| Python 구문 | GUI·CLI·설치기·검사 코드 4개 AST 검사 통과 |
| 문서 명령 | Bash 코드 블록 33개 구문 검사 통과. 설치 명령을 일괄 실행한 것이 아님 |
| 실행 아이콘 | 실제 설치본과 공백 포함 임시 경로에서 desktop-file-validate 통과 |
| 링크·지문 | 내부 문서 링크 대상 존재, 이전 시스템 참고 파일 6개 지문 일치 |
| 배포 내용 | 코드·아이콘·문서·검사·비밀정보 없는 설정 기록만 포함 |

### 이번 포장 전에 실제 화면에서 확인한 내용

- 자동 watcher가 iPad 해상도를 덮어쓰는 문제를 확인하고 중지/비활성화한 뒤 유지 상태를 확인했습니다.
- GUI 적용·유지·취소·시간 초과 및 선택기 종료 시 복구, 작은 화면에서 일반 창 맞춤·이전 배치 복구를 확인했습니다.
- 사용자 정의 모드 재등록과 NVIDIA MetaMode 전환, VNC scale 복구를 확인했습니다.
- 사용자는 원본 전송 후 글씨가 선명해졌다고 확인했고, 높이를 1050으로 조정한 최종 화면을 수용했습니다. 마지막 VNC framebuffer 갱신도1600×1050으로 확인했습니다.

위 내용은 기준 PC와 해당 사용자의 실제 기기에서 확인한 결과입니다. GUI를 패키징하면서 화면을 반복 전환하지 않았으며, 새 CLI의 실제 모드 변경 분기는 현재 화면을 유지하기 위해 모의 환경에서 검사했습니다.

### 영향과 미실시

현재 세션에는 설치 파일과 Codex CLI를 배포했습니다. 기존 실행 중인 VNC/GDM/Xorg는 재시작하지 않았고, 현재 해상도도 변경하지 않았습니다. 설치 영수증은 로컬 `~/.local/state/vnc-resolution-switcher/install-backups/`에 보관하며 게시하지 않습니다.

새 PC·다른 GPU·Ubuntu 24.04·Wayland·다중 출력·새 재부팅·케이블 연결/해제는 미실시입니다. 마지막 선택값의 재부팅 후 자동 복원 기능은 없습니다. 이전 2026-09-15 문서 검증 기록은 아래에 보존합니다.

---

# 2026-09-15 문서 검증 기록

검증일: 2026-09-15

## 통과한 검사

| 항목 | 결과 |
|---|---|
| 본문 구성 | 1~16장 존재 |
| 명령 예시 | Bash 코드 블록 24개 구문 검사 통과 |
| 문서 내 Python 예시 | 1개 구문 검사 통과 |
| 참고 셸 스크립트 | 2개 구문 검사 통과 |
| 참고 설정 6개 | 복사본·기록 지문·현재 원본의 SHA-256 모두 일치 |
| 내부 문서 링크 | 코드 블록을 제외한 링크의 대상 존재 확인 |
| 코드 블록 | 시작·종료 구분자 검사 통과 |
| 배포 범위 | 문서와 지정한 참고 설정만 포함 |
| 기본 접속 흐름 | VNC 직접 접속 명시, 필수 SSH 명령·패키지 제거, SSH·VPN은 선택 부록으로 분리 |

셸 예시는 bash -n 또는 sh -n으로 문법만 검사했습니다. 설치 명령·서비스 시작·화면 전환 명령을 실행하지 않았습니다. Python 예시도 구문 분석만 수행했습니다.

## 실시하지 않은 검사

- 새 PC에 패키지·설정 설치.
- Ubuntu 24.04 또는 다른 GPU에서의 실제 구동.
- 재부팅·모니터 케이블 연결·해제.
- 다중 모니터 자동 선택 구현·실행.
- VNC 뷰어 접속·GPU 렌더링·외부망 접속 시험.
- 모든 대상 PC에 대한 systemd·드라이버 호환성 검증.

위 항목은 대상 PC의 CHECKLIST.md에서 실제 결과를 기록해야 합니다. 문서 검증 통과를 장비 설치 성공으로 해석하지 않습니다.

## 현재 PC에 미친 영향

문서 폴더와 배포 ZIP만 작성했습니다. 조사한 시스템 설정 6개의 내용은 원본 지문과 일치합니다. 서비스 재시작, 재부팅, 비밀번호 변경, VPN 로그인은 수행하지 않았습니다. 비밀번호·인증 쿠키·개인키·토큰은 패키지에 없습니다.
